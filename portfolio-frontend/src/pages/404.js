import React from "react";

import LayoutMain from "_layouts/LayoutMain";
import { NotFound } from "_pages";

const NotFoundPage = () => {
  return (
    <LayoutMain>
      <NotFound />
    </LayoutMain>
  );
};

export default NotFoundPage;
